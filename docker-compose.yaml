services:
  # ===== 前端：AI 漫剧平台（nginx，同时反代 new-api） =====
  ai-manju:
    build:
      context: .
    image: ai-manju:latest
    container_name: ai-manju
    ports:
      - "8080:80"
    depends_on:
      - new-api
    restart: unless-stopped

  # ===== 后端：new-api（仅内部网络，通过前端 nginx 反代同源访问） =====
  new-api:
    image: calciumion/new-api:latest
    container_name: ai-manju-new-api
    expose:
      - "3000"
    volumes:
      - ./data/new-api:/data
    environment:
      - TZ=Asia/Shanghai
      - SQL_DSN=
      - SESSION_SECRET=change_this_session_secret
    restart: unless-stopped

  # ===== 可选：媒体代理（绕过签名 URL 的 CORS） =====
  media-proxy:
    build:
      context: .
      dockerfile: Dockerfile.proxy
    image: ai-manju-proxy:latest
    container_name: ai-manju-proxy
    ports:
      - "3001:3001"
    restart: unless-stopped
    profiles: ["proxy"]

  # ===== 可选：MySQL（替代 new-api 默认 SQLite） =====
  mysql:
    image: mysql:8
    container_name: ai-manju-mysql
    environment:
      - MYSQL_ROOT_PASSWORD=change_me
      - MYSQL_DATABASE=new-api
      - MYSQL_USER=newapi
      - MYSQL_PASSWORD=password
    volumes:
      - ./data/mysql:/var/lib/mysql
    restart: unless-stopped
    profiles: ["mysql"]
